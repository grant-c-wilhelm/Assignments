// let calculatorIsOn = true;
// function pressPowerButton  () {
//     if (calculatorIsOn) {
//       console.log('Calculator turning off.');
//       calculatorIsOn = false;
//     } else {
//       console.log('Calculator turning on.');
//       calculatorIsOn = true;
//     }
//       };

//       pressPowerButton();
//   // Output: Calculator turning on.

//       pressPowerButton();
//       // Output: Calculator turning off.

//to use a function is a 'stored procedure' that takes input and performs a taks/outputs a value
//to use a funtion it first must be DECLARED or DEFINED as either an expression or statement:
//you can have DECLARATIONS and EXPRESSIONS...EXPRESSIONS are special and violate the usual rules of reading
//from left to right and instead it looks for those functions before running the code so they can be anywhere 
//in your code

//execute a function using () syntax

//ther is no sppon
// var pen = {
//     magicInk: {
//         getColor: function () {
//             var colors = ['red', 'white', 'green', 'maroon'];
//             var randomIndex = Math.floor(Math.random() * colors.length);
//             return colors[randomIndex];
//         },

//         price= 2,
//         isGelPen: false,
//         write: function () {
//             console.log("I'm a " + this.color + " pen that costs $" + this.price);
//         }
//     }
// }

